import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const Color primary = Color(0xFF87A1FA);
  static const Color secondary = Color(0xFFCAC4D0);

  // Cores de fundo
  static const Color background = Color(0xFF1C1B1F);
  static const Color surface = Color(0xFF1A1D21);

  // Cores de status
  static const Color success = Color(0xFF74C365);
  static const Color error = Color(0xFFE24B4B);

  // Cores neutras
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
}
